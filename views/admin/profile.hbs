{{#if hasErrors}}
   <div class="alert callout">
    {{# each messages }}
        <p>{{this}}</p>
    {{/each }}
   </div>
{{/if}}
<div class="row">
    <div class="top-bar menu small-12 medium-12 large-12 columns">
        <div class="top-bar-title"><strong>Dashboard</strong></div>
        <div>
            <div class="top-bar-left">
                <ul class="dropdown menu" data-responsive-menu="drilldown medium-dropdown">
                    <li><a href="/admin/logout">Log Out</a></li>
                    <li><a href="/admin/products">Products</a></li>
                </ul>
            </div>
            <div class="top-bar-right">
                <ul class="dropdown menu">                    
                    <li><a href="/admin/search">Search</a></li>       
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="small-12 medium-12 large-12 columns">
        {{# if user}}
            <ul class="tabs" data-active-collapse="true" data-tabs id="collapsing-tabs">
                <li class="tabs-title is-active"><a href="#panel1c" aria-selected="true"><strong>{{greeting}}</strong></a></li>
                <li class="tabs-title"><a href="#panel2c"><strong>Contact</strong></a></li>
                <li class="tabs-title"><a href="#panel3c"><strong>Image</strong></a></li>
            </ul>

            <div class="tabs-content" data-tabs-content="collapsing-tabs">
                <div class="tabs-panel is-active" id="panel1c">
                    <table>
                        <thead>
                            <tr>
                                <th width="150">First Name</th>
                                <th width="150">Last Name</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>{{user.fname}}</td>
                                <td>{{user.lname}}</td>
                            </tr>                    
                        </tbody>
                    </table>
                </div>
                <div class="tabs-panel" id="panel2c">
                    <p>{{user.email}}</p>
                </div>
                <div class="tabs-panel" id="panel3c">
                    <img class="thumbnail profile-thumbnail" src="/graphics/products/vine.png">
                </div>
            </div>
        {{else}}
            <h1>User Profile</h1>
        {{/if}}
    </div>
</div>
   
